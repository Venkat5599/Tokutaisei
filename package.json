{
  "name": "blockchain-based",
  "version": "1.0.0",
  "description": "A production-ready decentralized scholarship management platform built on Ethereum Sepolia testnet that ensures complete transparency, prevents corruption, and automates fund distribution through smart contracts.",
  "type": "commonjs",
  "main": "index.js",
  "directories": {
    "doc": "docs",
    "test": "tests"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "hardhat run scripts/deploy.js --network sepolia",
    "deposit": "hardhat run scripts/deposit-funds.js --network sepolia",
    "compile": "hardhat compile"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^17.3.1"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^6.1.2",
    "hardhat": "^2.28.6"
  }
}
